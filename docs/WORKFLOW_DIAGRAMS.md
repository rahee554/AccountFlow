# AccountFlow Commands — Visual Workflow Guide

## 🔄 Complete Workflow Diagram

```
╔══════════════════════════════════════════════════════════════════════════╗
║                      ACCOUNTFLOW SETUP WORKFLOW                          ║
╚══════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────┐
│  Step 1: Install Package                                                │
│  $ composer require artflow-studio/accountflow                          │
└─────────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────────┐
│  Step 2: Link Package Files                                             │
│  $ php artisan accountflow:link --force                                 │
│                                                                          │
│  Package Files Copied:                                                  │
│  ├── /vendor/.../models → /app/Models/AccountFlow/                    │
│  ├── /vendor/.../migrations → /database/migrations/                   │
│  ├── /vendor/.../seeders → /database/seeders/  (individual files!) ✅ │
│  ├── /vendor/.../livewire → /app/Livewire/AccountFlow/               │
│  ├── /vendor/.../views → /resources/views/vendor/...                 │
│  └── /vendor/.../assets → /public/vendor/...                         │
└─────────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────────┐
│  Step 3: Run Database Setup (Interactive)                               │
│  $ php artisan accountflow:db                                           │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │ 📋 Step 1: Scan Migrations                                       │  │
│  │   Project: Found 2 AccountFlow migrations ✅                    │  │
│  │   Package: Found 0 migrations                                   │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                    ↓                                     │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │ 📋 Step 2: Scan Seeders                                          │  │
│  │   Project: Found 1 AccountFlow seeder ✅                        │  │
│  │   Package: Found 0 seeders                                      │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                    ↓                                     │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │ 📋 Step 3: Determine Sources (Priority: Project > Package)      │  │
│  │   Migrations: PROJECT ✅                                         │  │
│  │   Seeders: PROJECT ✅                                            │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                    ↓                                     │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │ ⚠️  Confirm to continue? (yes/no)                               │  │
│  │   → User confirms                                               │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                    ↓                                     │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │ 🚀 Step 4: Run Migrations                                        │  │
│  │   $ php artisan migrate                                          │  │
│  │   ✓ All AccountFlow tables created                              │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                    ↓                                     │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │ 🌱 Step 5: Run Seeders                                           │  │
│  │   $ php artisan db:seed                                          │  │
│  │   ✓ AccountFlow data seeded                                      │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                    ↓                                     │
│  ✅ Database Setup Complete!                                            │
│                                                                          │
│  📊 Summary:                                                             │
│     - Migrations Source: project                                        │
│     - Seeders Source: project                                           │
│     - Total files processed: 3                                          │
└─────────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────────┐
│  Step 4: Start Using AccountFlow                                        │
│  - Access UI in app                                                     │
│  - Create accounts, transactions, etc.                                  │
│  - Data persists in database                                            │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🔀 File Copy Logic Diagram

### Traditional Directory Symlink (Problem on Windows ❌)
```
Package Directory          Project Directory
    │                              │
    └─── link ───────────────────→ │
         (symlink)                 │
                                   ├─ AccountsTableSeeder.php ✅
                                   ├─ DatabaseSeeder.php ✅
                                   ├─ [link to seeders/] ❌ (nested!)
                                   └─ ...more files
```

**Problem:** If there's already a project `seeders/` folder, creating a link inside it creates nested junctions/symlinks.

---

### Individual File Copy (Solution ✅)
```
Package Directory          Project Directory
    │                              │
    ├─ AccountsTableSeeder.php     │
    │      │                       │
    │      └── copy ──────────────→├─ AccountsTableSeeder.php ✅
    │                              │
    └─ [other files]              └─ [existing files] ✅
        (not copied)                  (not affected)
```

**Solution:** Copy each seeder file individually to existing project folder. No nested links!

---

## 🎯 Decision Tree: Which Command to Use?

```
                    ┌─────────────────────────────────┐
                    │ Need to setup AccountFlow?      │
                    └─────────────────────────────────┘
                                 │
                 ┌───────────────┴───────────────┐
                 │                               │
        YES, First Time                 NO, Just Update
                 │                               │
                 ↓                               ↓
    ┌─────────────────────────┐    ┌──────────────────────────┐
    │ php artisan             │    │ php artisan              │
    │ accountflow:link        │    │ accountflow:link --force │
    │ --force                 │    │                          │
    └─────────────────────────┘    └──────────────────────────┘
                 │                               │
                 ├───────────────┬───────────────┤
                 │               │               │
                 ↓               ↓               ↓
         ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐
         │ Fresh        │  │ Just Add     │  │ Reset All        │
         │ Database?    │  │ Migrations?  │  │ (Production OK)  │
         └──────────────┘  └──────────────┘  └──────────────────┘
                 │               │                      │
                 YES             NO                     YES
                 │               │                      │
                 ↓               ↓                      ↓
    ┌──────────────────────┐  ┌────────────────────┐  ┌──────────────────┐
    │ php artisan          │  │ php artisan        │  │ php artisan      │
    │ accountflow:db       │  │ accountflow:db     │  │ accountflow:db   │
    │ --fresh              │  │ (interactive)      │  │ --fresh --force  │
    │ --force              │  │                    │  │                  │
    └──────────────────────┘  └────────────────────┘  └──────────────────┘
             │                        │                      │
             ↓                        ↓                      ↓
    ✅ All tables                ✅ Pending only         ✅ Auto complete
    recreated & seeded          migrations run           (CI/CD safe)
```

---

## 📊 Migration Priority System

```
┌─────────────────────────────────────────────────────────┐
│          Checking for AccountFlow Migrations            │
└─────────────────────────────────────────────────────────┘
                          │
                          ↓
          ┌───────────────────────────────┐
          │ Search in PROJECT folder:     │
          │ /database/migrations/         │
          │ (looking for "account" in     │
          │  filename)                    │
          └───────────────────────────────┘
                          │
                ┌─────────┴─────────┐
                │                   │
              FOUND              NOT FOUND
                │                   │
                ↓                   ↓
        ┌─────────────┐    ┌───────────────────┐
        │ USE PROJECT │    │ Search in PACKAGE │
        │ ✅ PRIORITY │    │ /vendor/.../db/   │
        └─────────────┘    │ migrations/       │
                           └───────────────────┘
                                   │
                         ┌─────────┴─────────┐
                         │                   │
                       FOUND              NOT FOUND
                         │                   │
                         ↓                   ↓
                 ┌─────────────┐    ┌──────────────┐
                 │ USE PACKAGE │    │ ⚠️  WARN     │
                 │ ✅ FALLBACK │    │ Not found    │
                 └─────────────┘    │ anywhere     │
                                    └──────────────┘
```

---

## 🔧 Command Parameters Guide

```
┌──────────────────────────────────────────────────────────┐
│         accountflow:link [--force]                       │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  --force (Optional)                                      │
│  ├─ Without flag: Ask before overwriting each file     │
│  └─ With flag: Overwrite all without asking            │
│                                                          │
│  Example:                                                │
│    $ php artisan accountflow:link             ← Interactive
│    $ php artisan accountflow:link --force     ← Auto
│                                                          │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│    accountflow:db [--force] [--fresh]                   │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  --force (Optional)                                      │
│  ├─ Without flag: Ask for confirmation before running  │
│  └─ With flag: Run without asking                      │
│                                                          │
│  --fresh (Optional)                                      │
│  ├─ Without flag: $ php artisan migrate                │
│  └─ With flag: $ php artisan migrate:fresh --force     │
│     (Drops all tables and recreates from scratch)       │
│                                                          │
│  Combinations:                                           │
│    $ php artisan accountflow:db                   ← Ask, migrate
│    $ php artisan accountflow:db --force          ← Auto, migrate
│    $ php artisan accountflow:db --fresh          ← Ask, fresh
│    $ php artisan accountflow:db --fresh --force  ← Auto, fresh
│                                                          │
└──────────────────────────────────────────────────────────┘
```

---

## 📈 Execution Flow Diagram

```
START: php artisan accountflow:db --fresh --force
   │
   ├─→ Check project migrations folder
   │   └─→ Found? ✅ YES
   │       └─→ Use project migrations
   │
   ├─→ Check project seeders folder
   │   └─→ Found? ✅ YES
   │       └─→ Use project seeders
   │
   ├─→ Display findings (No --force, so skip confirmation)
   │
   ├─→ RUN: php artisan migrate:fresh --force
   │   ├─→ Drop all tables
   │   ├─→ Run all migration files
   │   └─→ ✅ DONE
   │
   ├─→ RUN: php artisan db:seed
   │   ├─→ Call DatabaseSeeder
   │   ├─→ Include AccountsTableSeeder
   │   ├─→ Insert categories, accounts, settings
   │   └─→ ✅ DONE
   │
   └─→ Display summary
       ├─→ Migrations Source: project
       ├─→ Seeders Source: project
       ├─→ Total files: X
       └─→ ✅ SUCCESS!
```

---

## 🔍 File Detection Examples

### What Gets Detected? ✅

```
Project or Package migrations folder:
├── 0001_01_01_000000_create_users_table.php          ❌ (no "account")
├── 9900_create_accounts_tables.php                   ✅ ("account" found!)
├── 9901_add_columns_to_account.php                   ✅ ("account" found!)
└── 9902_add_account_features.php                     ✅ ("account" found!)

Project or Package seeders folder:
├── DatabaseSeeder.php                                 ❌ (no "account")
├── AccountsTableSeeder.php                            ✅ ("account" found!)
├── SettingsTableSeeder.php                            ❌ (no "account")
├── AccountFlowSeeder.php                              ✅ ("account" found!)
└── BranchSeeder.php                                   ❌ (no "account")
```

---

## 💾 Database State Transitions

```
INITIAL STATE
└─→ Empty database
    └─→ No tables

AFTER: php artisan accountflow:db (without --fresh)
└─→ Pending migrations run
    ├─→ New tables created
    ├─→ Existing tables unchanged
    └─→ Data intact

AFTER: php artisan accountflow:db --fresh
└─→ All tables DROPPED (destructive!)
├─→ All migrations re-run
├─→ All seeders re-run
└─→ Fresh database with default data

AFTER: php artisan accountflow:db --fresh --force
└─→ Same as --fresh
    └─→ But no confirmation (non-interactive)
```

---

## 🚨 Warning Indicators

```
⚠️  No AccountFlow migrations found in project
    → Next: Check package folder
    → Action: Run php artisan accountflow:link --force

⚠️  No AccountFlow seeders found in project
    → Next: Check package folder
    → Action: Run php artisan accountflow:link --force

⚠️  Could not create symlink/junction
    → Next: Falling back to file copy
    → Action: Should work fine (copy is safer than symlink)

⚠️  File already exists
    → Next: Skip file unless --force used
    → Action: Add --force to overwrite, or manually delete
```

---

## ✅ Success Indicators

```
✓ Copied {1} file(s): database/seeders → ...
  → Seeder successfully copied

✓ Linked: /path/to/file
  → Symlink/junction created successfully

✅ Migrations completed
  → All pending migrations ran successfully

✅ Seeding completed
  → All seeders ran successfully

✅ AccountFlow database setup completed successfully!
  → Everything completed without errors
```

---

**Visual Guide Complete!** 📊
