# Ponytail audit — artflow-studio/accountflow (G:\Web Development\appRepositories\Packages\AccountFlow) — 2026-09-17

delete: 5 Service classes (CategoryService, BudgetService, EquityService, WalletService, SettingsService — 272+293+219+241+170 = 1195 lines) that every real screen bypasses entirely. `CreateCategory`, `CreateBudget`, the Equity Livewire components, the Wallets Livewire components and the `Settings` Livewire component all call `Model::create()`/`update()`/`Setting::updateOrCreate()` directly — grep confirms zero real callers for any of these five classes outside their own facade/manager registration boilerplate. `SettingsService::get()` even re-introduces the exact "raw query on every check" bug the `Setting` model's own docblock says was fixed, and duplicates `Setting::default*()` and `FeatureService::isEnabled/enable/disable`. Delete the five classes, their `AccountFlowServiceProvider::SERVICES` entries, their `AccountFlowManager`/facade methods, and the now-pointless `ContainerTest` cases for them. [src/Services/CategoryService.php, src/Services/BudgetService.php, src/Services/EquityService.php, src/Services/WalletService.php, src/Services/SettingsService.php]

delete: `AccountsController` + `DefaultController` + `AnalyzeLivewireComponents` console command (66 + 37 + 133 = 236 lines) — a dead 0.2.x-migration cluster. Neither controller is wired into `routes/accountflow.php` (every route target is a Livewire component); the only place either class name appears again is inside `AnalyzeLivewireComponents`, which itself scans `app_path('Livewire/AccountFlow')` in the *host* app — a path the service provider's own comment says is "deliberately NOT publishable" any more, so the command can never find anything. `DefaultController`'s four getters duplicate the already-cached `Setting::defaultSalesCategoryId()`/`defaultAccountId()`/`defaultExpenseCategoryId()`/`routePrefix()`, minus the caching. `AccountsController::updateAccountBalance()` reads `config('accountflow.filter_column')`, a key that does not exist anywhere in `config/accountflow.php` — proof it has never actually run. [src/Http/Controllers/AccountsController.php, src/Http/Controllers/DefaultController.php, src/Console/Commands/AnalyzeLivewireComponents.php]

delete: `accountflow:db` command (225 lines). It "detects" whether migrations/seeders live in the project or the package by grepping filenames for the substring "account", prints which "source" it picked, then calls plain `migrate`/`migrate:fresh` regardless — Laravel already runs every registered migration (package + app) either way, so the entire detection step never changes what executes. `accountflow:migrate`, `accountflow:migrate:fresh` and `accountflow:install --migrate --seed` already cover every real case with a fraction of the code. [src/Console/AccountFlowDbCommand.php]

delete: `DelinkCommand` (`accountflow:delink`, 169 lines). Its own comment says it removes symlinks "created by accountflow:link" — no such command exists anywhere in this package (grep across src/docs finds nothing but that one comment). It's cleanup tooling for an install method that was never built. [src/Console/Commands/DelinkCommand.php]

delete: `CheckAccountflowStatus` (`accountflow:status`, 99 lines) duplicates `accountflow:diagnose`'s table-existence and feature-flag checks with a shallower version (row counts + a flag dump, no balance-drift/orphan/unposted-transfer checks). `Diagnose` supersedes it completely; keep one status command. [src/Console/Commands/CheckAccountflowStatus.php]

delete: `Purchase` + `PurchaseTransaction` models (50 + 35 lines), their two tables (`ac_purchases`, `ac_purchase_trx`, ~22 migration lines) and the `Feature::Purchases` flag. There is no `PurchaseService`, no controller, no Livewire screen, no route — grep for `Models\Purchase` inside `src/` returns nothing but the models' own files. It's a fully speculative module: tables and models for a feature whose UI was never written. [src/Models/Purchase.php, src/Models/PurchaseTransaction.php, database/migrations/9900_create_accounts_tables.php:150-172]

yagni: `ToggleFeature` (`accountflow:feature`) hand-rolls its own 12-entry feature-alias array and writes `DB::table('ac_settings')->update()` directly, duplicating `Feature::aliases()`/`Feature::tryParse()` and `FeatureService::set()` — the exact duplication the `Feature` enum's docblock says it exists to prevent ("0.2.x duplicated a 20-entry alias map... in two methods"). Replace the body with `app(FeatureService::class)->set($feature, $status === 'enable' || $status === 'enabled')`. ~50 lines gone, and it stops bypassing `Setting::put()`'s cache invalidation. [src/Console/Commands/ToggleFeature.php]

yagni: `AC` facade is byte-for-byte identical to the `Accountflow` facade (same accessor, same doc-block, no distinct behavior) and is used nowhere except its own test assertion. The `accountflow()` helper and `app('accountflow')` already give a short form. Delete `AC.php`. [src/Facades/AC.php]

native: `artflow-studio/snippets` is a hard `composer.json` `require` for exactly one guarded call site — `UniqueId::for()` — which already ships a complete, tested fallback for when `generateUniqueID()` doesn't exist (because the function only loads once that package's service provider boots). Since the fallback already has to exist and work standalone, drop the hard dependency and move it to `suggest` next to `spatie/laravel-permission`, or just always use the built-in generator. [src/Support/UniqueId.php, composer.json]

delete: `Setting::clearCache()` — a `@deprecated` one-line alias for `flushCache()` with zero remaining callers anywhere in src/resources/tests. [src/Models/Setting.php:189]

net: -2150 lines, -1 dep possible.
